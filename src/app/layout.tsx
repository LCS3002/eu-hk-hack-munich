// Root layout shell — next-intl middleware handles locale detection and redirects.
// All rendering happens inside src/app/[locale]/layout.tsx.
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return children
}
